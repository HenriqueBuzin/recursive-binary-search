#include <stdlib.h>
#include <stdio.h>

#include "binary_search.h"
#include "numbers.h"
#include "heap.h"

int binary_search(number_t ** numbers, int n_lines){
	int a = father(n_lines);
	int b = sizeof(numbers);
	return (a + b);
}

